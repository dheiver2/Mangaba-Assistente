# ===========================================
# BACKEND - ARQUIVO DE AMBIENTE PADRÃO
# ===========================================

# IMPORTANTE: Este arquivo é usado como fallback
# Para desenvolvimento: use .env.development
# Para produção: use .env.production

# Configurações do Servidor (fallback)
PORT=3001
NODE_ENV=development

# Configurações do Banco PostgreSQL (fallback para desenvolvimento)
DATABASE_URL=postgresql://neondb_owner:npg_fNV5yLmJt1su@ep-dark-tooth-adau8btp-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require

# Configurações JWT (fallback)
JWT_SECRET=mangaba_super_secret_jwt_key_2024_development_only
JWT_EXPIRES_IN=7d

# Configurações do Admin Padrão (fallback)
ADMIN_EMAIL=admin@mangaba.com
ADMIN_PASSWORD=Admin123!

# Configurações CORS (fallback)
FRONTEND_URL=http://localhost:5173

# INSTRUÇÕES:
# 1. Para desenvolvimento local: copie .env.development para .env.local
# 2. Para produção: configure as variáveis no seu provedor de hospedagem
# 3. Use arquivos específicos de ambiente (.env.development, .env.production)
# 4. Nunca commite arquivos .env com chaves reais de produção